# Sensitive files - never commit these
auth.json
*.auth
.build-certs/
*.pub
*-key.pem
*.pem

# Build artifacts and output directories
quadlet/output/
output/
output-*/
*.qcow2
*.iso
*.vmdk
*.vhd
*.raw

# Logs and temporary files
*.log
vm.pid
vm.monitor
/tmp/

# Environment files with secrets (use .env.example instead)
.env.local
.env.production

# VM Credentials (local configuration only)
quadlet/credentials.env
credentials.env
